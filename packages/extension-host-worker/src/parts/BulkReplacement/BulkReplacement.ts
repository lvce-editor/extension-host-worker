export const applyBulkReplacement = async (files, ranges, replacement): Promise<void> => {
  // eslint-disable-next-line no-console
  console.log({ files, ranges, replacement })
  throw new Error('not implemented')
}
